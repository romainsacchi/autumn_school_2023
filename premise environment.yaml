# in your terminal, run: conda env create -f premise environment.yaml

name: premise_env
channels:
  - romainsacchi
  - defaults
dependencies:
  - python=3.10
  - premise
  - jupyterlab
  - matplotlib
  - eidl
  - unfold